new Splide( '#committees',{
    type   : 'loop',
    lazyload: 'nearby',
} ).mount();

new Splide( '#about-slider',{
    type   : 'loop',
    arrows     : false,  
} ).mount();

new Splide( '#sponsor-container',{
    type   : 'loop',
    lazyload: 'nearby',
} ).mount();
